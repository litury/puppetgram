# Модуль автоматического вступления в каналы

Модуль для автоматического вступления в Telegram каналы с поддержкой приоритетов, фильтрации и интеграции с модулем комментирования.

## Структура модуля

```
src/modules/channelJoiner/
├── interfaces/           # Интерфейсы TypeScript
│   ├── IChannelJoiner.ts # Основные интерфейсы
│   └── index.ts          # Экспорт интерфейсов
├── services/            # Бизнес-логика
│   ├── channelJoinerService.ts     # Основной сервис вступления
│   ├── commentErrorExtractor.ts    # Извлечение каналов из ошибок комментирования
│   └── index.ts         # Экспорт сервисов
├── adapters/            # Адаптеры для форматирования
│   ├── joinResultAdapter.ts        # Форматирование результатов
│   └── index.ts         # Экспорт адаптеров
├── parts/               # Вспомогательные функции
│   ├── joinHelpers.ts   # Утилиты для работы с каналами
│   └── index.ts         # Экспорт функций
└── index.ts             # Главный экспорт модуля
```

## Запуск модуля

```bash
npm run join-channels
```

## Директории для файлов

### `input-join-targets/`

Основная директория для файлов с каналами для вступления:

- `example-join-targets.txt` - пример файла с форматами
- `failed_channels_*.txt` - автоматически созданные файлы с неудачными каналами

### `input-channels/`

Дополнительная директория (совместимость с другими модулями):

- Файлы из модуля комментирования
- Общие списки каналов

## Форматы файлов

### Базовые форматы каналов:

```
@username
https://t.me/username
username
```

### Дополнительные параметры:

```
@channel !high        # Высокий приоритет
@channel !medium      # Средний приоритет (по умолчанию)
@channel !low         # Низкий приоритет
@channel #error       # Из ошибок комментирования
@channel #manual      # Добавлен вручную
@channel #recommendation  # Из рекомендаций
```

### Комбинированные параметры:

```
@testovichan !high #error
@example_channel !medium #manual
https://t.me/another_channel !low #recommendation
```

## Возможности модуля

### 1. Вступление в каналы из файла

- Загрузка каналов из файлов в `input-join-targets/` и `input-channels/`
- Поддержка различных форматов каналов
- Настройка приоритетов и источников

### 2. Ручной ввод каналов

- Интерактивный ввод каналов через консоль
- Валидация формата каналов
- Автоматическое определение приоритета

### 3. Повтор неудачных попыток

- Автоматический поиск файлов с неудачными каналами
- Повышение приоритета для повторных попыток
- Сохранение новых неудачных каналов

### 4. Проверка доступа к каналам

- Анализ возможности вступления без реального вступления
- Определение типа канала (публичный/приватный)
- Проверка требований к одобрению

### 5. Выход из каналов

- Массовый выход из указанных каналов
- Подтверждение операций
- Обработка ошибок

## Настройки сессии вступления

### Основные параметры:

- **Максимум каналов**: Лимит каналов за сессию
- **Задержка между вступлениями**: Пауза в секундах
- **Случайный порядок**: Перемешивание списка каналов
- **Тестовый режим**: Симуляция без реального вступления

### Лимиты безопасности:

- **Часовой лимит**: 50 вступлений в час
- **Дневной лимит**: 200 вступлений в день
- **Обработка flood wait**: Автоматическая обработка ограничений Telegram

## Интеграция с модулем комментирования

Модуль автоматически интегрируется с модулем комментирования:

1. **Автоматическое извлечение каналов**: При ошибках доступа в комментировании
2. **Сохранение в правильную директорию**: `input-join-targets/`
3. **Предложение запуска**: Интерактивное предложение запустить модуль вступления
4. **Анализ ошибок**: Категоризация ошибок доступа

### Поддерживаемые ошибки:

- `CHAT_GUEST_SEND_FORBIDDEN` - требуется быть участником
- `USER_NOT_PARTICIPANT` - пользователь не участник канала
- `CHANNEL_PRIVATE` - приватный канал
- `CHAT_WRITE_FORBIDDEN` - запрещена запись в канал

## Экспорт результатов

### Форматы экспорта:

- **JSON**: Детальные результаты с метаданными
- **CSV**: Для анализа в Excel
- **TXT**: Текстовый отчет

### Автоматическое сохранение:

- Результаты сессий в `exports/`
- Каналы для повтора в `input-join-targets/`
- Детальные логи ошибок

## Типы ошибок и их обработка

### Автоматически повторяемые:

- `FLOOD_WAIT` - ограничения по времени
- `NETWORK_ERROR` - сетевые ошибки
- `OTHER` - прочие временные ошибки

### Требующие ручного вмешательства:

- `CHANNEL_NOT_FOUND` - канал не найден
- `BANNED` - пользователь заблокирован
- `PRIVATE_CHANNEL` - приватный канал
- `REQUIRES_APPROVAL` - требует одобрения

### Успешные операции:

- `ALREADY_MEMBER` - уже состоит в канале
- `JOINED` - успешно вступил

## Рекомендации по использованию

### Для эффективного вступления:

1. Начинайте с каналов высокого приоритета
2. Используйте случайные задержки (3-10 секунд)
3. Ограничивайте количество вступлений в день
4. Проверяйте доступ перед массовым вступлением

### Для избежания блокировок:

1. Не превышайте лимиты (50/час, 200/день)
2. Используйте тестовый режим для проверки
3. Обрабатывайте flood wait ограничения
4. Избегайте вступления в подозрительные каналы

### Для интеграции с комментированием:

1. Запускайте вступление после неудачного комментирования
2. Используйте автоматически созданные файлы
3. Повторяйте комментирование после вступления
4. Мониторьте статистику успешности

## Примеры использования

### Запуск с файлом:

```bash
npm run join-channels
# Выберите: "📁 Вступить в каналы из файла"
# Выберите файл из input-join-targets/
```

### Повтор неудачных:

```bash
npm run join-channels
# Выберите: "🔄 Повторить неудачные попытки"
# Выберите файл failed_channels_*.txt
```

### Проверка доступа:

```bash
npm run join-channels
# Выберите: "📊 Проверить доступ к каналам"
# Выберите источник каналов
```

## Логирование и мониторинг

Модуль предоставляет подробное логирование:

- Прогресс вступления в реальном времени
- Детальные ошибки с кодами
- Статистику успешности
- Рекомендации по улучшению

Все результаты сохраняются для последующего анализа и повтора неудачных операций.

 
 
 
 