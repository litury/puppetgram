# Модуль передачи владения каналами Telegram

Модуль для ротации каналов между аккаунтами для избежания flood-лимитов на 24 часа.

## Структура модуля

```
channelOwnershipRotator/
├── interfaces/           # Интерфейсы TypeScript
│   ├── IChannelOwnershipRotator.ts
│   └── index.ts
├── services/            # Основная бизнес-логика
│   ├── channelOwnershipRotatorService.ts
│   └── index.ts
├── adapters/            # Форматирование результатов
│   ├── ownershipResultAdapter.ts
│   └── index.ts
├── parts/               # Вспомогательные функции
│   ├── ownershipHelpers.ts
│   └── index.ts
├── index.ts             # Главный экспорт модуля
└── README.md           # Документация
```

## Основные компоненты

### 🔧 Сервисы

- **ChannelOwnershipRotatorService** - основной сервис для передачи владения каналами

### 📋 Интерфейсы

- **IOwnershipTransferRequest** - запрос на передачу владения
- **IOwnershipTransferResult** - результат передачи владения
- **IChannelInfo** - информация о канале
- **IUserInfo** - информация о пользователе

### 🎨 Адаптеры

- **OwnershipResultAdapter** - форматирование результатов для красивого вывода

### 🛠 Вспомогательные функции

- Создание Telegram клиентов
- Работа с идентификаторами каналов и пользователей
- Форматирование ошибок
- Валидация данных

## Запуск модуля

```bash
npm run transfer-channel
```

## Использование в коде

```typescript
import {
  ChannelOwnershipRotatorService,
  IOwnershipTransferRequest,
  OwnershipResultAdapter,
} from "./modules/channelOwnershipRotator";

const rotatorService = new ChannelOwnershipRotatorService();

const request: IOwnershipTransferRequest = {
  sessionString: "ваша_строка_сессии",
  channelIdentifier: "channel_username", // или ID
  targetUserIdentifier: "target_username", // или ID
  password: "ваш_пароль_2fa",
};

const result = await rotatorService.transferOwnershipAsync(request);

if (result.success) {
  console.log(OwnershipResultAdapter.formatSuccessResult(result));
} else {
  console.log(OwnershipResultAdapter.formatErrorResult(result));
}
```

## Возможности

### ✅ Основные функции

- Передача владения каналом другому пользователю
- Валидация данных перед передачей
- Получение информации о канале
- Получение информации о пользователе
- Красивое форматирование результатов
- Детальное логирование процесса

### ✅ Безопасность

- Маскирование сессий в логах
- Валидация всех входных данных
- Обработка ошибок 2FA
- Безопасное отключение клиентов

### ✅ Обработка ошибок

Модуль корректно обрабатывает все основные ошибки API:

- `PASSWORD_HASH_INVALID` - неверный пароль 2FA
- `PASSWORD_MISSING` - не настроена 2FA
- `PASSWORD_TOO_FRESH` - пароль изменен менее 24 часов назад
- `SESSION_TOO_FRESH` - сессия создана менее 24 часов назад
- `CHAT_ADMIN_REQUIRED` - нет прав администратора
- `USER_ID_INVALID` - неверный ID получателя
- `CHANNEL_PRIVATE` - приватный канал

## Требования

### 🔧 API конфигурация

Убедитесь что в `.env` файле указаны:

```env
API_ID=ваш_api_id
API_HASH=ваш_api_hash
```

### 📋 Требования для передачи

1. **Аккаунт-отправитель** должен быть владельцем канала
2. **Пароль 2FA** должен быть настроен и известен
3. **Сессия** не должна быть создана менее 24 часов назад
4. **Пароль** не должен быть изменен менее 24 часов назад
5. **Получатель** должен существовать и быть доступным

### ⚠️ Ограничения

- Передача владения работает только для каналов (не групп)
- Требуется 2FA пароль (обязательное требование API)
- Получатель может быть офлайн (не требует подтверждения)
- После передачи отменить операцию нельзя

## Пример использования

### 1. Простая передача

```bash
npm run transfer-channel
```

Выберите "Передать владение каналом" и следуйте инструкциям.

### 2. Проверка канала

```bash
npm run transfer-channel
```

Выберите "Получить информацию о канале" для проверки статуса владения.

### 3. Валидация данных

```bash
npm run transfer-channel
```

Выберите "Валидировать данные для передачи" для проверки без выполнения передачи.

## Интеграция с другими модулями

Модуль можно легко интегрировать с:

- **CommentPoster** - автоматическая ротация при flood-лимитах
- **ChannelManagement** - массовая ротация каналов
- **SessionGenerator** - управление множественными сессиями

## Структура согласно стандартам

Модуль полностью соответствует:

- ✅ **project-structure.mdc** - правильная организация файлов
- ✅ **frontend-coding-standards.mdc** - соглашения по коду
- ✅ **Архитектура проекта** - event-driven, модульность
- ✅ **Безопасность** - маскирование данных, валидация

## Логирование

Все операции подробно логируются:

```
🔄 [transfer_1642584736_a8f2k9] Начинаю передачу владения каналом...
📺 Канал: my_channel
👤 Получатель: target_user
🔐 Сессия: 1BAAO*****tring

📋 [transfer_1642584736_a8f2k9] Шаг 1: Подключение к Telegram...
📋 [transfer_1642584736_a8f2k9] Шаг 2: Получение информации о текущем пользователе...
...
✅ [transfer_1642584736_a8f2k9] Передача владения завершена успешно!
```

## Расширения

Модуль легко расширить для:

- Массовой ротации каналов
- Планировщика ротации
- Интеграции с базой данных
- API интерфейса
- Мониторинга flood-лимитов
