# 📸 Директория фотографий профиля

Эта директория предназначена для хранения фотографий профиля, которые используются модулем `profileManager`.

## 📁 Структура

```
photos/
├── avatars/          # Готовые аватары для ботов
├── temp/            # Временные файлы при обработке
├── uploaded/        # Загруженные пользователем фото
└── README.md        # Этот файл
```

## 🔧 Использование

### Поддерживаемые форматы

- **JPEG** (.jpg, .jpeg) - рекомендуется для фотографий
- **PNG** (.png) - рекомендуется для изображений с прозрачностью
- **WebP** (.webp) - современный формат с хорошим сжатием

### Требования к изображениям

- **Максимальный размер:** 10 MB
- **Рекомендуемое разрешение:** 512x512 пикселей (квадрат)
- **Минимальное разрешение:** 160x160 пикселей

### Примеры использования

```typescript
// Загрузка фото профиля
const photoRequest: IProfilePhotoUpdateRequest = {
  sessionString: "your_session_string",
  photoPath: "./src/modules/profileManager/photos/avatars/bot1.jpg",
};

const result = await profileService.uploadProfilePhotoAsync(photoRequest);
```

## 🚨 Важные замечания

1. **Безопасность:** Не храните здесь персональные фотографии
2. **Лицензии:** Убедитесь, что у вас есть права на использование изображений
3. **Размер:** Большие файлы замедляют загрузку профиля
4. **Очистка:** Регулярно очищайте папку `temp/` от старых файлов

## 📋 Автоматизация

Модуль автоматически:

- ✅ Проверяет формат и размер файла
- ✅ Создает превью изображения
- ✅ Сохраняет метаданные о загрузке
- ✅ Удаляет временные файлы после обработки
