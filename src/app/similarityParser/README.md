# Channel Similarity Parser

Модуль для парсинга похожих каналов Telegram с поддержкой рекурсивного поиска и фильтрации дубликатов.

## ✨ Новые возможности

### 🚫 Фильтрация дубликатов

- **Автоматическое исключение** уже спарсенных каналов
- **Загрузка фильтров** из файлов при запуске
- **Автоматическое добавление** новых результатов в фильтр

### 📁 Структура директорий модуля

```
src/modules/channelSimilarityParser/
├── exports/                          # 📤 Результаты парсинга
│   ├── {channel}_usernames_*.txt     # Файлы с юзернеймами
│   ├── {channel}_detailed_*.md       # Подробные отчеты
│   └── {channel}_*_partial_*.txt     # Частичные результаты (при FloodWait)
│
├── filter-duplicates/                # 🚫 Фильтрация дубликатов
│   ├── README.md                     # Документация по фильтрации
│   └── parsed-channels/              # Файлы с уже спарсенными каналами
│       ├── example-parsed.txt        # Пример файла
│       └── {channel}_parsed_*.txt    # Автоматически созданные фильтры
│
├── services/                         # 🔧 Основная логика
├── adapters/                         # 📊 Форматирование результатов
├── interfaces/                       # 📝 TypeScript интерфейсы
├── parts/                           # 🧩 Вспомогательные функции
└── utils/                           # 🛠️ Утилиты (включая фильтрацию)
```

## 🚀 Использование

### Запуск парсера

```bash
npm run parse-similar
```

### Фильтрация дубликатов

#### Автоматическая фильтрация

1. **При запуске** - автоматически загружаются все файлы из `filter-duplicates/parsed-channels/`
2. **При обработке** - найденные каналы сравниваются с фильтрами
3. **После завершения** - новые результаты добавляются в фильтр

#### Ручное добавление файлов фильтрации

Поместите `.txt` файлы с юзернеймами в `filter-duplicates/parsed-channels/`:

```
# Комментарии игнорируются
@channel1
@channel2
channel3  # @ не обязателен
```

## 📊 Обработка ошибок

### FloodWait Protection

- **Критические FloodWait** (>5 минут) останавливают выполнение
- **Частичные результаты** автоматически сохраняются
- **Файлы с пометкой** `_partial_` создаются в exports/

### Логирование

- **Подробные логи** процесса фильтрации
- **Статистика исключений** дубликатов
- **Информация о файлах** фильтрации

## 🎯 Основные функции

### ChannelSimilarityParserService

- `parseSimilarChannelsAsync()` - основной метод парсинга
- `validateChannelAccessAsync()` - проверка доступности канала
- Автоматический экспорт результатов
- Сохранение частичных результатов при ошибках

### Система фильтрации (utils/duplicateFilter.ts)

- `loadExclusionFilters()` - загрузка файлов фильтрации
- `filterDuplicateChannels()` - исключение дубликатов
- `addToExclusionList()` - добавление новых результатов в фильтр

## 🔧 Конфигурация

### Параметры поиска

- **Источник**: канал для поиска похожих (@channel_name)
- **Лимит**: максимальное количество результатов
- **Рекурсия**: поиск по найденным каналам (глубина до 50)
- **Фильтры**: минимум/максимум подписчиков

### Автоматическое поведение

- ✅ **Автоэкспорт** результатов в TXT и MD форматах
- ✅ **Автофильтрация** дубликатов при каждом запуске
- ✅ **Автосохранение** частичных результатов при ошибках
- ✅ **Автодобавление** новых результатов в фильтр

## 📝 Примеры использования

### Первый запуск

```bash
npm run parse-similar
# Введите @example_channel
# Результаты: exports/{channel}_usernames_*.txt
# Фильтр: filter-duplicates/parsed-channels/{channel}_parsed_*.txt
```

### Последующие запуски

```bash
npm run parse-similar
# Введите @another_channel
# Каналы из предыдущих результатов автоматически исключаются
# Показываются только новые уникальные каналы
```

### Ручное добавление в фильтр

```bash
# Создайте файл filter-duplicates/parsed-channels/blacklist.txt
echo "@spam_channel1" >> filter-duplicates/parsed-channels/blacklist.txt
echo "@spam_channel2" >> filter-duplicates/parsed-channels/blacklist.txt
```

## 🛡️ Безопасность

- **FloodWait обработка** предотвращает блокировку аккаунта
- **Задержки между запросами** для соблюдения лимитов API
- **Graceful shutdown** при критических ошибках
- **Частичное сохранение** для восстановления после ошибок
