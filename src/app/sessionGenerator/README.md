# Модуль генерации сессий Telegram

Модуль для создания, валидации и управления сессиями Telegram согласно стандартам компании.

## Структура модуля

```
sessionGenerator/
├── interfaces/           # Интерфейсы модуля
│   ├── ISessionGenerator.ts
│   └── index.ts
├── services/            # Основные сервисы
│   ├── sessionGeneratorService.ts
│   └── index.ts
├── adapters/            # Адаптеры для взаимодействия
│   ├── interactiveAuthAdapter.ts
│   ├── sessionStorageAdapter.ts
│   ├── sessionResultAdapter.ts
│   └── index.ts
├── parts/               # Вспомогательные функции
│   ├── sessionHelpers.ts
│   └── index.ts
├── index.ts             # Главный экспорт модуля
└── README.md           # Документация
```

## Основные компоненты

### 🔧 Сервисы

- **SessionGeneratorService** - основной сервис для генерации и валидации сессий
- **InteractiveAuthAdapter** - адаптер для интерактивной авторизации через консоль
- **SessionStorageAdapter** - адаптер для сохранения сессий в файловую систему
- **SessionResultAdapter** - адаптер для форматирования результатов

### 📋 Интерфейсы

- **ISessionGenerator** - основной интерфейс генератора сессий
- **ISessionGenerationOptions** - опции генерации сессии
- **ISessionGenerationResult** - результат генерации сессии
- **IInteractiveAuthHandler** - интерфейс для интерактивной авторизации
- **ISessionStorage** - интерфейс для хранения сессий

### 🛠 Вспомогательные функции

- Валидация номеров телефонов
- Форматирование номеров телефонов
- Работа с файлами сессий
- Маскирование конфиденциальных данных
- Генерация имен файлов

## Использование

### 1. Через главное приложение

```bash
npm start
# Выберите "3 - Генерация сессий Telegram"
```

### 2. Через отдельный скрипт

```bash
npm run generate-session
```

### 3. Программное использование

```typescript
import {
  SessionGeneratorService,
  InteractiveAuthAdapter,
  SessionStorageAdapter,
  ISessionGenerationOptions,
} from "./modules/sessionGenerator";

// Создание сервисов
const authAdapter = new InteractiveAuthAdapter();
const storageAdapter = new SessionStorageAdapter();
const sessionGenerator = new SessionGeneratorService(authAdapter);

// Опции генерации
const options: ISessionGenerationOptions = {
  apiId: Number(process.env.API_ID),
  apiHash: process.env.API_HASH!,
  deviceModel: "Desktop",
  systemVersion: "Windows 10",
  appVersion: "1.0.0",
};

// Генерация сессии
const result = await sessionGenerator.generateSession(options);

// Сохранение сессии
const filename = await storageAdapter.saveSession(result);
```

## Возможности

### ✅ Создание сессий

- Интерактивная авторизация через номер телефона
- Поддержка двухфакторной аутентификации
- Автоматическое получение информации о пользователе
- Валидация данных на всех этапах

### ✅ Управление сессиями

- Сохранение сессий в файлы JSON
- Просмотр списка сохраненных сессий
- Валидация существующих сессий
- Удаление неактуальных сессий

### ✅ Безопасность

- Маскирование номеров телефонов в логах
- Безопасное хранение сессий
- Валидация входных данных
- Обработка ошибок

### ✅ Удобство использования

- Интерактивный интерфейс
- Подробные инструкции
- Форматированный вывод результатов
- Автоматическое создание директорий

## Файлы сессий

Сессии сохраняются в директории `./data/accounts/sessions/` в формате:

```json
{
  "sessionString": "1BAAOMTQ5LjE1NC4xNjcuNTABu...",
  "phoneNumber": "+79123456789",
  "userId": 123456789,
  "username": "example_user",
  "firstName": "Иван",
  "lastName": "Иванов",
  "generatedAt": "2024-01-15T10:30:00.000Z",
  "isValid": true
}
```

Имя файла формируется автоматически: `session_{номер_телефона}_{дата}.json`

## Требования

- Node.js 16+
- TypeScript 4.5+
- Переменные окружения:
  - `API_ID` - ID приложения Telegram
  - `API_HASH` - Hash приложения Telegram

## Безопасность

⚠️ **ВАЖНО:**

- Никогда не делитесь SESSION_STRING с третьими лицами
- Добавьте `.env` в `.gitignore`
- Храните файлы сессий в безопасности
- Регулярно проверяйте валидность сессий

## Структура согласно стандартам

Модуль полностью соответствует:

- **proj-struct-guideline.md** - структура файлов и директорий
- **web-coding-guideline.md** - правила именования и кодирования

### Особенности реализации:

- Использование camelCase для файлов и переменных
- Префикс `_` для входящих параметров функций
- Префикс `p_` для приватных переменных
- Комментарии на русском языке
- Использование async/await
- Интерфейсы для всех структур данных
- Разделение по папкам: interfaces, services, adapters, parts
